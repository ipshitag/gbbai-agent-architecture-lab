{
  "schema": 1,
  "description": "Classify a sports-related user query into one of the predefined intent categories (e.g., summarize, historical, scores, etc.)",
  "execution_settings": {
    "default": {
      "max_tokens": 1000,
      "temperature": 0.0,
      "top_p": 0.0,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0
    }
  },
  "input_variables":
   [
      {
        "name": "input",
        "type": "string",
        "description": "The user’s sports-related query or statement to be classified"
      }
    ],
    "default_values": {
    "input": ""
  }
}